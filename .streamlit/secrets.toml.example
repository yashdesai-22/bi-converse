# Paste this into the "Secrets" field when deploying to Streamlit Community Cloud
# (Settings → Secrets in your app's dashboard). Do NOT check the real secrets.toml in.

HF_TOKEN = "hf_your_token_here"

# Optional overrides
# HF_MODEL = "Qwen/Qwen2.5-Coder-32B-Instruct"
# HF_PROVIDER = "auto"
